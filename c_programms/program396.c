#include <stdio.h>
int main()
{
    char *str = "Ganesh";

    printf("%c", *str);
    str++;

    printf("%c", *str);
    str++;

    printf("%c", *str);
    str++;

    printf("%c", *str);
    str++;

    printf("%c", *str);
    str++;

     printf("%c", *str);
    str++;

    return 0;
}